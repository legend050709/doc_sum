```table-of-contents
```
# 概述
![](attachments/Pasted%20image%2020240422110243.png)


# 速率

![](attachments/Pasted%20image%2020240422110414.png)

# 带宽
## 定义
带宽在百度百科中定义：在单位时间内从网络中的某一点到另一点所能通过的**“最高数据率”**。
计算机网络的带宽是指网络可通过的最高数据率，即**每秒多少比特**（常用的单位是bps(bit per second)）。

![](attachments/Pasted%20image%2020240422103941.png)

## 使用
描述带宽时常常把“**比特/秒**”省略。例如，带宽是100M，实际上是100Mbps，这里的Mbps是指兆位/s。

但是我们平时下载软件的速度的单位是**Byte/s（字节/秒**）。这里涉及到Byte和bit的换算，二进制数系统中每个0或1就是一个位(bit)，位是数据存储的最小单位，其中8bit就称为一个字节（Byte）。

因此我们在办理宽带的时候，**100M的带宽表示100Mbps，理论的的网络下载速度只有12.5M Bps，实际可能还不足10MBps**，这是因为受用户计算机性能、网络设备质量、资源使用情况、网络高峰期、网站服务能力、线路衰耗，信号衰减等多因素的影响，实际网速是无法到达理论网速的。


# 时延
**时延：** 指数据（一个报文或分组）从网络（或链路）的一端传送到另一端所需要的总时间，它由4部分构成;`发送时延、传播时延、处理时延、排队时延`。

`总时延=发送时延+传播时延+处理时延+排队时延`

![](attachments/Pasted%20image%2020240422110603.png)

## 分类
**网络时延**包括了**发送时延、传输时延、处理时延、排队时延**这四大部分。在实际中我们主要考虑发送时延与传播时延。

![](attachments/Pasted%20image%2020240422104226.png)


### 发送时延

节点将分组的所有比特推向（传输）链路所需要的时间，即从发送分组的第一个比特算起，到该分组的最后一个比特推向传输链路所需要的时间。也称`传输时延`。

如果用L比特表示分组的长度，用R bps表示从路由器A到路由器B的链路传输速率，发送时延则是L/R。
![](attachments/Pasted%20image%2020240422110907.png)


实际的发送时延通常在毫秒到微秒级。
![](attachments/Pasted%20image%2020240422111306.png)

### 传播时延
电磁波在信道中传播一定的距离需要花费的时间，即一个比特从链路的一端传播到另一端所需要的时间。
传播时延是指报文在**实际的物理链路上传播数据所需要的时间**。

传播时延等于两台路由器之间的距离除以传播速率，即传播时延是D/S，其中D是两台路由器之间的距离，S是该链路的传播速率。

![](attachments/Pasted%20image%2020240422111241.png)

实际传播时延在毫秒级。

### 排队时延
**背景**
分组在进入路由器后要先在输入队列中排队等待处理。因为路由器可能也同时在处理其他链路传过来的数据，所以需要等待。然后终于等到路由器来处理你的数据了，路由器处理完你的数据后，会给你一个转发端口，然后你就拿着这个端口号又在输出队列中排队等待转发，因为等待转发的不止你一个啊，路由器一下会处理很多的数据，这就产生了排队时延。

![](attachments/Pasted%20image%2020240422111150.png)

一个数据包的排队时延取决于当前队列中是否有其它报文在传输。
如果该队列是空的，并且当前没有其他报文在传输，则该报文的排队时延为0；
实际的排队时延通常在毫秒到微秒级。

### 处理时延
数据在交换结点为存储转发而进行的一些必要的处理所花费的时间。
例如：分析分组的首部、从分组中提取数据部分、进行差错检验或查找适当的路由等

![](attachments/Pasted%20image%2020240422111410.png)

一般高速路由器的处理时延通常是微秒或更低的数量级。

## P99
### 定义
p 的含义是 percentile  n.百分位，百分位数。
p50, p90, p99（或者写作 pct 50，pct90，pct 99）是数据聚合统计一种方式，即百分比。
p50：数据集按升序排列，第50分位置大的数据（即升序排列后排在50%位置的数据）。  
p90：数据集按升序排列，第90分位置大的数据（即升序排列后排在90%位置的数据）。  
p99：数据集按升序排列，第99分位置大的数据（即升序排列后排在99%位置的数据）。

### 作用
有什么实际含义和用处？  
经常用来衡量服务响应延迟。

以最常用的 p99 为例，它衡量了 99% 的情况下，能达到的最大服务响应延迟，99%的延迟都低于这个数值，即绝大多数情况下的最差响应情况。

以p90为例，它衡量了 90% 的情况下，能达到的最大服务响应延迟，90%的延迟都低于这个数值，即多数情况下的最差响应情况。

## 往返时延（RTT）

![](attachments/Pasted%20image%2020240422111854.png)

## 应用
### 时延带宽积
时延带宽积（又称为以比特为单位的链路长度），也称为在途字节数， 指发送端发送的第一个比特即将到达终点时，发送端已经发出了多少个比特/字节。

时延带宽积 = 传播时延×信道带宽

![](attachments/Pasted%20image%2020240422111812.png)

# 抖动
## 定义
**抖动：**网络抖动是指**最大延迟与最小延迟的时间差**，比如你访问一个网站的最大延迟是10ms，最小延迟为5ms，那么网络抖动就是5ms；

## 应用
**抖动可以用来评价网络的稳定性，抖动越小，网络越稳定**；

尤其是我们在打游戏的时候，需要网络具有较高的稳定性，否则会影响游戏体验。

## 抖动原因
关于网络抖动产生的原因：如果网络发生拥塞后，排队时延会影响端到端的延迟，可能造成从路由器A到路由器B的延迟忽大忽小，造成网络的抖动；


# 丢包
丢包:简单来说，丢包是指一个或多个数据包的数据不能通过网络到达目的地。如果接收器发现数据丢失，将根据队列序号向发送器发出请求，并重新传输丢包。

## 丢包原因
丢包的原因有很多，最常见的可能是网络拥塞，数据流量太大，网络设备无法处理，自然会丢失一些数据包。

比如：**路由器缓存**的容量是有限的，分组到达已满队列的路由器时，将会被丢弃。

## 丢包率

丢包率，显然就是丢弃的包，除以发送方发送的所有的数据包。
丢包率 = 丢弃的包数量/所有的数据包 =（所有的数据包-成功接收的包）/所有的数据包 = 1-成功接收的包/所有的数据包

# 容量
## 并发连接数
## 最大连接数
# 其他
# 参考
```bash
# 计算机网络的性能指标（速率、带宽、吞吐量、时延、往返时延、时延带宽积、信道利用率）
https://blog.csdn.net/weixin_43914604/article/details/104541219
```