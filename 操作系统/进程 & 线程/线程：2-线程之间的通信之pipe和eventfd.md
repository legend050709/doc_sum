```table-of-contents
```
# pipe

# eventfd
## 介绍
`eventfd` 是一个文件描述符，类似于管道、socket 等 I/O 对象。通过它，用户可以在进程间（父子进程或其他有共享资源的进程之间）、线程间发送事件通知。它的核心思想是一个计数器，可以原子地增加或减少。当计数器的值发生变化时，可以通过 `select` 或 `poll` 等 I/O 多路复用机制监听该事件，从而通知其他进程或线程。


# 参考
```bash

```