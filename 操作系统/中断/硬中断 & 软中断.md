```table-of-contents
```
# 定义
中断，顾名思义，就是一种可以打断处理器当前运行的程序，转而去运行其他的程序的方式。
当某些特定事件（event）被触发，如外部设备完成特定操作，或者执行了未知的指令，或者访问了无法访问的内存，处理器就会触发一个中断请求（IRQ, Interrupt Request），来对其进行处理。通过这种方式，我们可以快速的对这些事件进行响应。


# 中断上下部分
为了解决**中断处理程序执行过长和中断丢失**的问题，Linux 将中断处理过程分成了两个阶段，也就是上半部和下半部：
上半部用来快速处理中断，它在中断禁止模式下运行，主要处理跟硬件紧密相关的或时间敏感的工作。
下半部用来延迟处理上半部未完成的工作，通常以内核线程kirqsoftd的方式运行。


## 中断上半部分（硬中断）：完全屏蔽中断（不可中断） + 快速执行

对上半部来说，既然是快速处理，其实就是要把网卡的数据读到内存中之后，产生硬中断，中断处理程序然后更新一下硬件寄存器的状态（表示数据已经读好了），最后再发送一个软中断信号，通知下半部做进一步的处理。
总结：**上半部的功能是“登记中断”。硬中断需要能尽可能快地执行，然后再重新打开硬中断。**
当一个中断发生时，他就把设备驱动程序中中断例程的下半部挂到该设备的下半部执行队列中去，然后就没事情了--等待新的中断的到来。
上半部之所以要快，是因为它是**完全屏蔽中断的**，如果它不执行完，其它的中断就不能被及时的处理，只能等到这个中断处理程序执行完毕以后。所以，要尽可能多得对设备产生的中断进行服务和处理，中断处理程序就一定要快。

## 中断下半部分（软中断）：可中断 + 延迟执行
有些中断事件的处理是比较复杂的，所以中断处理程序必须多花一点时间才能够把事情做完。可怎么样化解在短时间内完成复杂处理的矛盾呢，这时候Linux引入了下半部的概念。

下半部和上半部最大的不同是：
**下半部是可中断的，而上半部是不可中断的。**
下半部几乎做了中断处理程序所有的事情，因为上半部只是将下半部排到了他们所负责的设备的**中断处理队列**中去，然后就什么都不管了。
下半部一般所负责的工作是察看设备以获得产生中断的事件信息，并根据这些信息（一般通过读设备上的寄存器得来）进行相应的处理。
如果有些时间下半部不知道怎么去做，他就使用著名的鸵鸟算法来解决问题--说白了就是忽略这个事件。

由于下半部是可中断的，所以在它运行期间，如果其它的设备产生了中断，这个下半部可以暂时的中断掉，等到那个设备的上半部运行完了，再回头来运行它。

下半部被软中断信号唤醒后，需要从内存中找到网络数据，再按照网络协议栈，对数据进行逐层解析和处理，直到把它送给应用程序。

可以把软中断系统想象成一系列**内核线程**（每个 CPU 一个），这些线程执行针对不同事件注册的处理函数（handler）。

### 中断下半部分（软中断）内核线程

实际上，上半部会打断 CPU 正在执行的任务，然后立即执行中断处理程序。而下半部以内核线程的方式执行，并且每个 CPU Core 都对应一个软中断内核线程，名字为 “ksoftirqd/CPU 编号”，比如说， 0 号 CPU 对应的软中断内核线程的名字就是 ksoftirqd/0。

不过要注意的是，软中断不只包括了刚刚所讲的硬件设备中断处理程序的下半部，一些内核自定义的事件也属于软中断，比如内核调度和 RCU 锁（Read-Copy Update 的缩写，RCU 是 Linux 内核中最常用的锁之一）等。

## 小结
所以，这两个阶段你也可以这样理解：
**上半部直接处理硬件请求，也就是我们常说的硬中断，特点是快速执行；
而下半部则是由内核触发，也就是我们常说的软中断，特点是延迟执行。**


# 分类
中断理论上分为三类：**外部中断（External Interrupt）**，**软件中断（Software Interrupt）** 和 **内部中断（Internal Interrupt）**。不同的平台的分类经常有些许不同，但是大体都差不多，比如，intel就把它分类为：外部中断，可忽略的硬件中断，软件中断和异常。


# 参考
```c
# Linux 中断（IRQ/softirq）基础：原理及内核实现（2022）
https://arthurchiao.art/blog/linux-irq-softirq-zh/

```