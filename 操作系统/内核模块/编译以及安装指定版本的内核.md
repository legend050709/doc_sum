```table-of-contents
```
# 前言
我们通常所说的 Linux 其实指的是 Linux 内核（kernel），而 Redhat、Debian、Ubuntu 这些称之为发行版。
kernel 是操作系统的最底层，负责整个硬件的驱动，以及提供系统所需的核心功能，包括防火墙机制、是否支持 LVM 或 Quota 等文件系统等等。
在主机开机的时候，其实就是利用开机管理程序加载这个内核文件，来检测硬件，在内核加载适当的驱动程序后，系统才能够顺利的运行。
# 准备工作
## 内核编译的目的
- 新功能的需求
- 原有内核过于臃肿
- 新的硬件的适配

## 内核源码获取
### 通过发行版获取
如果只是想在当前的内核中添加某些功能，可以通过当前发行版来获取源码；
其优点是：发行版中含有厂商设定好的预设值， 我们可以轻松的了解到与核心及模块有关的各项设定项目的参数值， 以便对我们的编译工作降低难度；

### 通过内核官网获取
    
虽然通过发行版获取的内核文件能够降低我们编译的难度，但是无法对行硬件提供良好的支持，因此就需要从内核官网下载最新的内核源码进行编译，以获得最新的驱动支持
kernel 官网：[http://www.kernel.org/](http://www.kernel.org/)
最新稳定版：[https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.2.13.tar.xz](https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.2.13.tar.xz)

以 4.9.39 版本为例

```bash
wget https://cdn.kernel.org/pub/linux/kernel/v4.x/linux-4.9.39.tar.xz
```
# 内核编译
## 处理内核源码
以 4.9.39 版本为例

```bash
wget https://cdn.kernel.org/pub/linux/kernel/v4.x/linux-4.9.39.tar.xz
xz -d linux-4.9.39.tar.xz 
tar xvpf linux-4.9.39.tar
```
我们不清楚下载的原码中有没有保留目标文件 (*.o) 以及相关的配置文件存在， 此时我们可以通过下面的方式来处理掉这些编译过程的目标文件以及配置文件。
```bash
 # 删除前一次编译过程的残留数据
make mrproper && make clean
```

## 安装编译环境
```bash
# Debian/Ubuntu 
sudo apt install build-essential 
sudo apt install ncurses-dev
# RedHat/CentOS 
sudo yum groupinstall "development tools"
sudo yum install -y ncurses-devel
```
## 创建编译config参数
在 `/boot/` 目录下存在一个名为 `config-xxx` 的文件，该文件其实就是内核功能列表文件，接下来我们就需要创建这个文件，后续的编译就是通过这个文件来处理的。

以下三种方法可生成编译参数文件
- 使用当前系统的配置参数
- 问答式全新配置
- 载入目前配置并对新配置进行询问时配置（建议使用）

**（1）make oldconfig**
通过使用已存在的` .config` 文件内容，使用该文件内的设定值为默认值，只将新版本内核中的新功能选项，列出让用户选择， 可以简化内核功能的选择过程，对于作为升级内核原码后的功能选择来说，是非常好用的一个选项。

**（2）make menuconfig**
最常使用的是命令行模式下面可以显示纯文本界面的方式。
![](attachments/Pasted%20image%2020240324151023.png)
- 在选择选项时，如果为 `[*]` 或者 `<*>` 则表示编译进内核；如果为 `<M>` 则表示编译成模块
- 使用空格键选择或取消选项
- 选择完毕后，选择【save】，即可生成`.config` 文件

**（3）通过既有的设置来完成内核选项与功能的选择**
如果我们只需要对内核做很小的修改，那么我们就可以使用现有的内核设置进行微调即可.
```bash
1> 复制原来的配置文件到新下载的内核目录
cp /boot/config-3.14.0-162.23.1.el9_1.x86_64 .config

2> 执行功能选择
make menuconfig

3> 使用左右键，选择【load】，导入配置文件，并做修改

4> 修改完毕后，选择【save】，即可生成新的 .config 文件，而原来的文件则自动重命名为 .config.old

```

## 内核的编译与安装
### 编译内核与内核模块
在功能选择完毕后，接下来就是编译内核与核心，该工作非常简单，只需要三条命令即可（当然也可以合并为一条命令）
```bash
# 生成未经压缩的内核
[root@vm-rocky9 linux-6.2.13]# make vmlinux
# 仅生成核心模块
[root@vm-rocky9 linux-6.2.13]# make modules
# 生成经压缩过的核心(默认)
[root@vm-rocky9 linux-6.2.13]# make bzImage

# 以上三步合并执行
## 表示使用16线程同时编译
[root@vm-rocky9 linux-6.2.13]# make -j 16 all
or
make -j $(nproc)
```
在 **/boot/** 目录下的内核文件都是经过压缩过的，因此，上述的操作中比较常用的是 modules 与 bzImage 这两个，bzImage 可以生成压缩过后的内核， 也就是我们用来做系统启动的信息。
### 安装模块
模块的存放路径为： `/lib/modules/$(uname -r)` ，如果同一版本的模块被反复编译时，就会产生冲突，可以通过以下方法处理。
- 先将旧的模块目录重命名，然后才安装内核模块到目标路径中去

前面通过`make modules` 或者 `make -j 16 all` 完成了内核模块的编译。
安装模块只需要在当前路径执行 `make modules_install` 命令即可。

### 安装新内核
```bash
make install
```
执行完成之后，查看`grubby --info ALL`可以看到新编译的内核。

### 选择启动内核
```bash
查看: grubby --info ALL
选择：grubby --set-default-index=x
检查：grubby --default-kernel

重启： reboot
检查： uname -r 
```
# 参考
```bash

```