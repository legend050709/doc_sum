```table-of-contents
```

# 概述

![](attachments/image%20(20).png)

```bash

一个机房内的2个节点之间的通信：可能要经过7跳（不包含2个终端）
tor-lf-sp-ssp-sp-lf-tor

tor-lf 是pod 级，tor-lf-sp 是cluster 级，tor-lf-sp-ssp 是机房az 级；过了ssp 那一层就跨机房了，本机房内的都会在ssp 下；
```
# 参考
```bash

```