```table-of-contents
```
# 总览

![](attachments/Pasted%20image%2020260718210559.png)

C++类和对象（一）：https://blog.csdn.net/chenlong_cxy/article/details/117194830
C++类和对象（二）：https://blog.csdn.net/chenlong_cxy/article/details/117307465
C++类和对象（三）：https://blog.csdn.net/chenlong_cxy/article/details/117530132

# 面向对象和面向过程
首先我们先给出以下结论：  
 **C语言是面向过程的，关注的是过程**，分析出求解问题的步骤，通过函数调用逐步解决问题。  
 **C++是基于面向对象的，关注的是对象**，将一件事拆分成不同的对象，靠对象之间的交互完成。

举例说明面向过程和面向对象的**区别**：
我们就**外卖系统**来看看面向过程和面向对象之间的区别：  
 面向过程，我们的关注点应该是**用户下单**、**骑手接单**以及**骑手送餐**这三个过程。  
 面向对象，那我们的关注点应该就是**客户**、**商家**以及**骑手**这三个类对象之间的关系。


# 类
## 定义

## C++中的struct 和 class
C++中：
struct定义的类中所有的成员默认为public，struct中也可以有成员函数（不同于C语言中的struct）。
class定义的类中左右的成员默认为private；

## 访问限定符
### public
### protected
### private


## 类的成员变量（属性）

## 类的成员函数（方法）

## 类的封装
## 类的作用域

## 类的实例化：对象

### 匿名对象

## this 指针

## 运算符重载（Operator Overloading）函数

### 运算符重载函数的调用 和 申明的理解
#### 范例：`=`运算符的重载
#### 范例：`[]`运算符的重载
```C++
namespace cl
{
	//模拟实现string类
	class string
	{
	public:
		//[]运算符重载（可读可写）
		char& operator[](size_t i)
		{
			assert(i < _size); //检测下标的合法性
			return _str[i]; //返回对应字符
		}
		//...
	private:
		char* _str;       //存储字符串
		size_t _size;     //记录字符串当前的有效长度
		//...
	};
}
int main()
{
	cl::string s("hello");
	s[3] = 'x';    //引用返回，支持外部修改
	return 0;
}

```

**理解**：
调用时：`[]`的左边是重载函数对应的类的实例对象，`[]`中的元素是参数。
整体：`xx[yy]：就是xx对象调用`[]`运算符重载函数的返回值，其中yy是函数参数`。比如此范例的`s[3]`


#### 范例：`()`运算符的重载
```C++
class Add
{
public:
	Add(int base)
		:_base(base)
	{}
	int operator()(int num)
	{
		return _base + num;
	}
private:
	int _base;
};
int main()
{
	int base = 1;

	//函数对象
	Add add1(base);
	add1(1000);

	//lambda表达式
	auto add2 = [base](int num)->int
	{
		return base + num;
	};
	add2(1000);
	return 0;
}

```

**理解**：
调用时：`()`的左边是重载函数对应的类的实例对象，`()`中的元素是参数。
整体：`xx(yy)：就是xx对象调用`()`运算符重载函数的返回值，其中yy是函数参数`。比如此范例的`add1(1000)`;

# 父类 & 子类 & 友元
## 基类（父类）
## 派生类（子类）

# 类的默认成员函数
## 构造函数
### 默认构造函数
### 带参数的构造函数
### 拷贝构造函数

## 析构函数

### 构造函数中调用虚函数为何不安全？


# 赋值运算
## 深拷贝和浅拷贝
![](attachments/Pasted%20image%2020260718213916.png)



# 对象和类
## 类是对象的模板
类是对象的模板或蓝图，它定义了对象的属性和行为。

## 对象是类的实例

# 参考
```bash

```